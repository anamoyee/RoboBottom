tcrutils>=13.1.26
hikari[speedups]>=2.3.1,<3.0.0
hikari-arc>=2.1.0,<3.0.0
hikari-miru>=4.2.0,<5.0.0
pydantic>=2.10.6
psutil>=6.0.0

# uvloop is used to speed up asyncio loop on non-nt systems.
uvloop>=0.20.0; sys_platform != 'win32'
